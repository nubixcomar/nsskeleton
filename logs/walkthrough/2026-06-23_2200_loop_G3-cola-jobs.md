# Walkthrough — Fase G3: cola de jobs generalizada

**Fecha y hora:** 2026-06-23 22:00 | **Agente:** loop/dev-backend (Claude Code) | **Modelo:** claude-opus-4-8
**Sprint:** S9 (v1.8) | **Versión:** 1.8

---

## Resumen ejecutivo
Se agregó una cola de jobs genérica (no solo emails) con reintentos, worker CLI/cron y panel
de monitoreo.

## Cambios realizados
- **Migración** `jobs` (handler, payload, status, attempts/max, available_at, error…).
- **Contrato** `App\Jobs\Job` (`handle(array)`) + handler demo `LogJob` + registro
  `config/job_handlers.php` (nombre => clase).
- **`App\Services\JobQueue`**: `push` (con delay), `work` (reserva con lock optimista +
  ejecuta), reintentos con **backoff lineal** y `failed` al agotar intentos, `retry`,
  `forget`, `stats`, `recent`.
- **Worker**: CLI `system/console/queue.php work --max=N` + job interno de cron `queue:work`.
- **Panel** `JobQueueController` (`/admin/jobs`): tarjetas por estado, lista, reintentar /
  eliminar / "Procesar ahora" + ítem de menú.

## Verificación
- `php -l` OK.
- **Suite**: **204/204 PASS** (+4 feature `JobQueue`: válido→done; inexistente max=1→failed;
  max>1→retry/pending con backoff; retry+forget).
- **E2E (MySQL 3307)**: encolé `demo:log` → worker CLI procesó (1 ok) → el log se escribió;
  el panel muestra stats y el job; el menú "Cola de jobs" aparece. (Jobs/log demo limpiados.)

## Pendientes / follow-ups
- **G4** Webhooks salientes — cierra el Track G y los 13 ítems.

## Referencias
- `system/app/Services/JobQueue.php`, `system/app/Jobs/*`, `system/console/queue.php`,
  `system/app/Controllers/Admin/JobQueueController.php`, `tests/feature/JobQueueTest.php`.
